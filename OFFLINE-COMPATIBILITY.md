# DiamondBank-OG offline-player API compatibility

Fully offline-safe and omitted from the table: `getBankShards`, `registerEventListener`, `diamondsToShards` and `shardsToDiamonds`.

Legend: 🔴 breaks the economy or permanently blocks economy operations | 🟠 produces wrong results, loses or duplicates value, or prevents intended offline behavior | 🟡 minor API inconsistency or limited degradation

| Method | Offline | Note |
|---|---|---|
| `getBaltop`<br>K:128, J:187 | Partial | 🔴 `offset` reaches SQL unvalidated, from `DiamondBankAPIKotlin.kt:128` and `DiamondBankAPIJava.kt:187` through `CachingBalanceManager.kt:179` into `OFFSET ?` at `PostgreSQL.kt:313`. PostgreSQL rejects a negative OFFSET, and the `DatabaseException` from `PostgreSQL.kt:327` reaches `handleError` at `DiamondBankAPIKotlin.kt:133` and `DiamondBankAPIJava.kt:192`. The disable is the intended response; the missing bounds check is the defect. Trigger: any negative `offset`. Offline consequence: the only API that enumerates disconnected players is also the one that turns caller arithmetic into an outage. Blast radius: every method returns `EconomyDisabledException` until an admin re-enables. *Fix: clamp `offset` to zero in both APIs.* |
| `consumeFromPlayer`<br>K:146, J:219 | **No** | 🟠 Offline inventory and ender chest support does not exist. This is the only item-consuming API and it returns `PlayerNotOnlineException` at `DiamondBankAPIKotlin.kt:151` and throws it at `DiamondBankAPIJava.kt:225` before reading any balance, because `InventorySnapshot.kt:26` to `:31` needs a live locked `PlayerInventory`. `OfflinePlayer` in purpur-api exposes no inventory or ender chest accessor, and `inventory_shards` and `ender_chest_shards` are mirrored counts rather than items, so no current code path can debit a disconnected player's items. Trigger: any offline target. Blast radius: unavailability only, with no partial state change. *Fix: the planned custom `PlayerInventory` implementation, backed by persisted data so it can serve a disconnected player.* |
| `getInventoryShards` K:85, J:107<br>`getEnderChestShards` K:89, J:119<br>`getTotalShards` K:92, J:131<br>`getAllShards` K:96, J:143<br>`getBaltop` K:128, J:187<br>`playerPayPlayer` K:199, J:277 | Partial | 🟠 Counting and extraction disagree on which slots hold currency, in both directions. Offhand is invisible to both: `InventoryExtensions.kt:10` counts via `Inventory.all`, which `CraftInventory` implements over `getStorageContents`, returning only the 36 storage slots, and `InventorySnapshot.kt:33` to `:34` copies into a 36 slot chest so slots 36 to 40 are truncated, with `InventorySnapshot.kt:51` restoring `storageContents` only. Uncolored shulker contents are counted by `InventoryExtensions.kt:14` to `:16` but not extractable, because `InventorySnapshotUtils.kt:25`, `:32` and `:58` use `removeItem`, which does not descend; colored variants are excluded from both. Trigger: currency held in the offhand, or inside an uncolored shulker box. Offline consequence: both errors freeze into the persisted columns at `Events.kt:105`, `:107` and `:113` and are the only figures available for a disconnected player, so a caller sizing a charge from `getTotalShards` can under-count offhand currency and over-count shulker currency, then hit `InsufficientFundsException`. Blast radius: every inventory or ender chest figure, and `getBaltop` ordering. *Fix: the planned custom `PlayerInventory` implementation, applied to counting and extraction alike so the two agree on offhand and shulker contents.* |
| `addToPlayerBankShards` K:26, J:34<br>`subtractFromPlayerBankShards` K:56, J:66<br>`playerPayPlayer` K:199, J:277 | Partial | 🟠 Two different existence rules, neither exposed. `addToPlayerBankShards` checks nothing, going from `DiamondBankAPIKotlin.kt:36` and `DiamondBankAPIJava.kt:40` to the upsert at `PostgreSQL.kt:136`, so any UUID gets a funded row. `playerPayPlayer` then refuses that same UUID via `hasPlayedBefore()` at `DiamondBankAPIKotlin.kt:215` and `DiamondBankAPIJava.kt:287`. Trigger, through supported API calls only: credit a never-joined UUID, then try to pay it. Offline consequence: a connected UUID validates itself, so only offline targets are exposed, and callers cannot tell the cases apart because `hasEntry` at `CachingBalanceManager.kt:195` is on neither API; `subtractFromPlayerBankShards` reports an unknown UUID and a broke player identically, since `PostgreSQL.kt:290` to `:295` reads a missing row as zero and `CachingBalanceManager.kt:67` returns `InsufficientBalanceException(0)`. Blast radius: the balance can be zeroed but the row cannot be deleted, so it keeps its place in the `getBaltop` ordering at `PostgreSQL.kt:311`. *Fix: expose `hasEntry` and apply one rule across all three.* |
| `playerPayPlayer`<br>receiver may be offline<br>K:199, J:277 | Partial | 🟠 In Kotlin the routine failure escapes the `Result` contract. `CommonOperations.kt:37` to `:39` throws `InsufficientFundsException` instead of returning it, so the `getOrElse` at `DiamondBankAPIKotlin.kt:222` never runs and the exception leaves a function declared `Result<Unit>`. `DiamondBankAPIKotlin.kt:191` to `:193` still documents a returned `CouldNotRemoveEnoughException`, a type that no longer exists, with a `notRemoved` field actually named `short`. Trigger: bank plus extractable inventory does not cover the amount. Offline consequence: paying a disconnected receiver is this method's main offline use, and a Kotlin caller handling the shortfall with `onFailure` crashes instead, losing the `short` value needed to retry. J is unaffected and declares the type at `DiamondBankAPIJava.kt:274`. `consumeFromPlayer` inherits the same escape through `DiamondBankAPIKotlin.kt:159`. Blast radius: caller-side crash. *Fix: return it as `Result.failure` and correct the K documentation.* |

Verified against commit `3d392ea`. Adversarial review by Codex `gpt-5.6-sol` at `model_reasoning_effort=xhigh`, 10 findings raised, 9 accepted and 1 rejected in part after source verification.
